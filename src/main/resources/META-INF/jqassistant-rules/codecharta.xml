<jqassistant-rules xmlns="http://schema.jqassistant.org/rule/v2.2"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://schema.jqassistant.org/rule/v2.2 https://jqassistant.github.io/jqassistant/current/schema/jqassistant-rule-v2.2.xsd">

    <concept id="codecharta:Metrics">
        <description>Assigns metrics to code elements.</description>
        <cypher><![CDATA[
            MATCH
              (element)-[:HAS_METRICS]->(metrics:CodeCharta:Metrics)
            WITH
              element, keys(metrics) AS metrics
            UNWIND
              metrics AS metric
            RETURN
              count(element) AS Elements, collect(DISTINCT metric) AS Metrics
            ]]></cypher>
        <verify>
            <aggregation/>
        </verify>
    </concept>

</jqassistant-rules>
